<div id="<%= dom_id book %>">
  <p>
    <strong><%= t('attributes.title') %></strong>
    <%= book.title %>
  </p>

  <p>
    <strong><%= t('attributes.memo') %></strong>
    <%= book.memo %>
  </p>

  <p>
    <strong><%= t('attributes.author') %></strong>
    <%= book.author %>
  </p>

  <p>
    <strong><%= t('attributes.picture') %></strong>
    <%= image_tag(book.picture_url) if book.picture.present? %>
  </p>

</div>
